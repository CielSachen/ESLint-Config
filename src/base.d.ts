import type { Linter } from "eslint";

/** The config for linting JavaScript, Node.js, and TypeScript features. */
declare const cielsachenBaseConfig: readonly Linter.Config[];

export default cielsachenBaseConfig;
